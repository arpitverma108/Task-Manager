{
  "name": "team-task-manager-backend",
  "version": "1.0.0",
  "description": "Team Task Manager REST API — Node.js + Express + SQLite",
  "main": "server.js",
  "scripts": {
    "start": "NODE_ENV=production node server.js",
    "dev": "NODE_ENV=development nodemon server.js",
    "prod": "npm run build && npm start",
    "lint": "echo 'Add ESLint or similar for production'",
    "test": "echo 'Add tests here'"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "sql.js": "^1.12.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "express-rate-limit": "^7.2.0",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
