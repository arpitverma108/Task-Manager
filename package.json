{
  "name": "team-task-manager",
  "version": "1.0.0",
  "description": "Team Task Manager — monorepo root for Railway deployment",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
